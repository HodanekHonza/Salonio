meta {
  name: getBookingByClientId
  type: http
  seq: 5
}

get {
  url: {{Host}}/booking/:bookingId/client/:clientId
  body: none
  auth: bearer
}

params:path {
  clientId: 
  bookingId: 
}

auth:bearer {
  token: {{Token}}
}

settings {
  encodeUrl: true
}
