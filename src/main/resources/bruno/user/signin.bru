meta {
  name: signin
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/auth/signin
  body: json
  auth: inherit
}

body:json {
  {
      "username": "testuser",
      "password": "testpass"
  }
}

settings {
  encodeUrl: true
}
